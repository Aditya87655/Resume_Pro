// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Add any other Next.js specific configurations here if you need them later.
  // For example, if you explicitly need to enable features or configure image optimization.
  // For now, an empty object means default Next.js behavior.
};

export default nextConfig;